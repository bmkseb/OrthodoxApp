/** Web stub — TrackPlayer is native-only; mezmur uses the YouTube player on web. */
export function isTrackPlayerNativeAvailable(): boolean {
  return false;
}

export function getTrackPlayerModule(): null {
  return null;
}
