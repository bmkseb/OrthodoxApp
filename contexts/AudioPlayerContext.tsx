/**
 * Audio player context — canonical re-export.
 * @see ./audio-player-context.tsx
 */
export {
  AudioPlayerProvider,
  useAudioPlayer,
  usePlayback,
  type AudioTrack,
  type PlaybackTrack,
} from '@/contexts/audio-player-context';
