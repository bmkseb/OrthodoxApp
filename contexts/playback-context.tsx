/** @deprecated Import from `@/contexts/audio-player-context` instead. */
export {
  AudioPlayerProvider,
  AudioPlayerProvider as PlaybackProvider,
  useAudioPlayer,
  useOptionalAudioPlayer,
  usePlayback,
  type AudioTrack,
  type AudioTrack as PlaybackTrack,
} from '@/contexts/audio-player-context';
